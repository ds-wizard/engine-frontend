{
  "name": "wizard-style-builder",
  "version": "3.11.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "init": "./scripts/init.sh",
    "start": "NODE_ENV=production node index.js",
    "clean": "rm -rf ./src",
    "test": "NODE_ENV=test jest --silent=false --testTimeout=10000 --runInBand --detectOpenHandles"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.1",
    "fs-extra": "^10.1.0",
    "helmet": "^5.0.2",
    "morgan": "^1.10.0",
    "node-sass": "^7.0.1"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "supertest": "^6.2.3"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "//node_modules/"
    ]
  }
}
